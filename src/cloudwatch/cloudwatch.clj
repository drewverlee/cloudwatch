(ns cloudwatch.cloudwatch
  "FIXME: my new org.corfield.new/scratch project.")

(defn exec
  "Invoke me with clojure -X cloudwatch.cloudwatch/exec"
  [opts]
  (println "exec with" opts))

(defn -main
  "Invoke me with clojure -M -m cloudwatch.cloudwatch"
  [& args]
  (println "-main with" args))
